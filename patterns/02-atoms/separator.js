import React from 'react';
import { View } from 'react-native';

// Local
import { utilStyles } from '@base/styles/utils';

export const Separator = () => <View style={utilStyles.separator} />;

export default Separator;
